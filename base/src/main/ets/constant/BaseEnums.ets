export enum StorageKey {
  UI_CONTEXT = "UI_CONTEXT",
  GLOBAL_INFO = 'GLOBAL_INFO',
  HOME_PAGE_CONTEXT = "HOME_PAGE_CONTEXT"
}

export enum ProductSeriesEnum {
  HPR = 'HPR', // fordable PC
  VDE = 'VDE',
}